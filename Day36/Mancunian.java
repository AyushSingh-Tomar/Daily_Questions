/*#include<bits/stdc++.h>
 using namespace std; const int N = 1e6; vector <int> pandigs[10]; vector < int> all_permutations[10]; void pandigitals(){     vector<int> arr[10];     for(int i = 1; i < 10; i++){         for(int j = 1; j <= i; j++)         arr[i].push_back(j);         sort(arr[i].begin(),arr[i].end());         do{             int num = 0;             for(int k = 0; k < i; k++){                 num = (num * 10 ) + arr[i][k];             }             all_permutations[i].push_back(num);         }while(next_permutation(arr[i].begin(),arr[i].end()));     } } 
 int count_digits(int n) { string num = to_string(n);  
 return num.size(); } signed main(){     pandigitals();     int t;     cin >> t;     while(t--){         int l,r,cnt=0;         cin >> l >> r;         int lc = count_digits(l), rc = count_digits(r);         for (int i =lc; i <= rc; i++){             for( auto val : all_permutations[i]){                 if (l <= val && r >= val) cnt++;             }         }         cout << cnt << endl;     }     return 0; } */
